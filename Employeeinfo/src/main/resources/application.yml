eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka/

server:
  port: 9092
spring:
  application:
    name: Employeeinfo
  datasource:
    url: jdbc:mysql://localhost:3306/microserviceproject1
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true